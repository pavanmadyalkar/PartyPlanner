# Gallery Media

Use these folders for the gallery page:

- `assets/gallery/photos/`
- `assets/gallery/videos/`

Gallery page currently uses hardcoded file paths inside `gallery.html` (`mediaLibrary` array).
After adding files, update that array.
